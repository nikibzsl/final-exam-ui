
/* const headerComponent = function() {
   return `
    <header>
	<h3 class="logo">${books.logo}</h3>
	<span class="material-symbols-outlined" id="menu">
		menu
		</span>
    </header>
    `
}

const cardComponent = function (sub, title, text) {
    return `
    <div class="books-card-container">
        <h3 class="book-sub">${sub}</h3>
        <h2 class="book-title">${title}</h2>
        <p class="book-text">${text}</p>
    </div>
    <div class="button-container"
        <button class="read-more">${books.button}</button>
     <span class="material-symbols-outlined">
        arrow_forward
        </span>
    </div>
    `
}



const loadEvent = function () {
    const rootElement = document.getElementById("root")



    rootElement.insertAdjacentHTML("beforeend", headerComponent())
    rootElement.insertAdjacentHTML("beforeend", cardComponent())
    rootElement.insertAdjacentHTML("beforeend", cardsContainer())

}

window.addEventListener("load", loadEvent) */